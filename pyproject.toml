[project]
name = "mlab-notebooks"
version = "0.1.0"
description = "M-Lab notebooks and tutorials: Monthly Stats workshop"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "pyarrow>=14.0",
    "requests>=2.28",
    "matplotlib>=3.7",
    "seaborn>=0.13",
    "ipywidgets>=8.0",
    "jupyter>=1.0",
    "ipykernel>=6.0",
]

[tool.uv]
# The notebooks import these at runtime; they are not used outside monthlystats/.
# Kept parallel to monthlystats/requirements.txt and environment.yml.