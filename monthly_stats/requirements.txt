# M-Lab Monthly Stats notebook dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# mlab-iqb brings pandas, pyarrow, and requests as transitive dependencies,
# so you don't need to install those separately.

# IQB library — data access and score calculation
git+https://github.com/m-lab/iqb.git#subdirectory=library

# Visualization and notebook UI
matplotlib>=3.7
seaborn>=0.13
ipywidgets>=8.0

# Optional: improves country name display (falls back to built-in list if absent)
# pycountry
