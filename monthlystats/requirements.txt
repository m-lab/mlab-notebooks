# M-Lab Monthly Stats workshop notebooks — dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Notebooks use one loading pattern (manifest → URL → pd.read_parquet) and
# interactive widgets + plotting.

pandas>=2.0
pyarrow>=14.0
requests>=2.28
matplotlib>=3.7
seaborn>=0.13
ipywidgets>=8.0